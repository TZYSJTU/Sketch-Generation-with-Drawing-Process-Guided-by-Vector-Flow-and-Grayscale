# Pencil-drawing
just run cat.py to draw the cat.
just run dog.py to draw the dog.
just run girl.py to draw the girl.

Of course , you can change the input file and adjust hyperparameters .

All the output is in file "./output".

(this code is  for Windows)